<?xml version="1.0" encoding="utf-8"?>
<!-- shortcuts_view.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/border"
    android:padding="5dp">
    <TextView
        android:id="@+id/text_view_shortcuts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:text="Your Shortcuts:"
        />
    <!-- RecyclerView to display the list of shortcuts -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewShortcuts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Button to create new shortcuts -->
    <Button
        android:id="@+id/buttonCreateShortcut"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create Shortcut"
        android:visibility="gone"
        android:layout_gravity="center"/>

</LinearLayout>